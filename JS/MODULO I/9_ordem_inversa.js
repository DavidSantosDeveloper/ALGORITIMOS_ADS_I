import * as readsync from 'readline-sync'

/*
   9. Leia 2 números (A, B)
    e escreva-os em ordem inversa (B, A). 
*/

// entrada
const numeroA = Number(readsync.question('Número A: '))
const numeroB = Number(readsync.question('Número 2: '))


// processamento 

// saída
console.log(`numero B: ${numeroB} e numero A: ${numeroA}`)